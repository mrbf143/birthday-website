<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Happy Birthday! ✨</title>
    
    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400..900;1,400..900&family=Plus+Jakarta+Sans:wght@300;400;600&display=swap" rel="stylesheet">

    <!-- Libraries -->
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/0.160.0/three.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/gsap.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/canvas-confetti@1.9.2/dist/confetti.browser.min.js"></script>

    <style>
        :root {
            --aurora-1: hsla(333, 70%, 55%, 0.4);
            --aurora-2: hsla(282, 82%, 54%, 0.3);
            --aurora-3: hsla(210, 89%, 60%, 0.3);
            --aurora-4: hsla(35, 90%, 60%, 0.3);
        }

        body {
            margin: 0;
            padding: 0;
            background-color: #0c0a09;
            color: #f8fafc;
            font-family: 'Plus Jakarta Sans', sans-serif;
            overflow: hidden;
            height: 100vh;
        }

        /* Animated Aurora Background */
        .aurora-container {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -2;
            filter: blur(100px);
            opacity: 0.8;
        }

        .aurora-layer {
            position: absolute;
            width: 150%;
            height: 150%;
            top: -25%;
            left: -25%;
            background: 
                radial-gradient(circle at 20% 30%, var(--aurora-1) 0%, transparent 40%),
                radial-gradient(circle at 80% 20%, var(--aurora-2) 0%, transparent 40%),
                radial-gradient(circle at 40% 80%, var(--aurora-3) 0%, transparent 40%),
                radial-gradient(circle at 70% 70%, var(--aurora-4) 0%, transparent 40%);
            animation: aurora-flow 20s ease-in-out infinite alternate;
        }

        @keyframes aurora-flow {
            0% { transform: rotate(0deg) scale(1); }
            100% { transform: rotate(10deg) scale(1.1); }
        }

        /* Glassmorphism Styles */
        .glass-card {
            background: rgba(12, 10, 9, 0.6);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
            border-radius: 24px;
        }

        .serif-title {
            font-family: 'Playfair Display', serif;
            background: linear-gradient(to right, #ffafbd, #ffc3a0, #ffffff);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        /* Button Styling */
        .btn-primary {
            background: linear-gradient(45deg, #ec4899, #ef4444);
            box-shadow: 0 0 20px rgba(236, 72, 153, 0.4);
            transition: all 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            border: none;
            color: white;
            font-weight: 600;
        }

        .btn-primary:hover {
            transform: scale(1.05) translateY(-2px);
            box-shadow: 0 0 30px rgba(236, 72, 153, 0.6);
        }

        .btn-primary:active {
            transform: scale(0.95);
        }

        /* Progress Bar */
        .progress-container {
            width: 200px;
            height: 4px;
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(5px);
            border-radius: 10px;
            overflow: hidden;
        }

        #progress-bar {
            width: 20%;
            height: 100%;
            background: linear-gradient(to right, #ec4899, #ef4444);
            transition: width 0.6s ease;
        }

        /* Polaroid Effect */
        .polaroid-frame {
            background: white;
            padding: 12px 12px 40px 12px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.3);
            transform: rotate(-3deg);
            transition: transform 0.1s ease-out;
        }
        
        .polaroid-img {
            width: 100%;
            height: 200px;
            object-fit: cover;
            background: #eee;
        }

        .step-content {
            display: none;
            opacity: 0;
            transform: translateY(20px);
        }

        .step-content.active {
            display: flex;
        }

        #three-canvas {
            position: fixed;
            top: 0;
            left: 0;
            z-index: -1;
            pointer-events: none;
        }

        .bento-card {
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(255, 255, 255, 0.08);
            border-radius: 16px;
            padding: 1.5rem;
        }
    </style>
</head>
<body>

    <canvas id="three-canvas"></canvas>

    <div class="aurora-container">
        <div class="aurora-layer"></div>
    </div>

    <!-- Progress Navigation -->
    <div class="fixed top-8 left-0 w-full flex justify-center z-50">
        <div class="progress-container">
            <div id="progress-bar"></div>
        </div>
    </div>

    <main class="relative h-screen w-full flex items-center justify-center p-4">
        
        <!-- Step 1: Welcome -->
        <section id="step1" class="step-content active flex-col items-center text-center max-w-md glass-card p-8 md:p-12">
            <div class="text-6xl mb-6 animate-bounce">💖</div>
            <h1 class="serif-title text-4xl md:text-5xl font-bold mb-4">Hey Beautiful,</h1>
            <p class="text-gray-300 mb-8 text-lg">I built a little world for you, just to bring a smile to your face on your special day.</p>
            <button onclick="nextStep(2)" class="btn-primary px-8 py-3 rounded-full text-lg">Let's Begin</button>
        </section>

        <!-- Step 2: Happy Birthday -->
        <section id="step2" class="step-content flex-col items-center text-center max-w-md glass-card p-8 md:p-12">
            <div class="text-6xl mb-6">🎉</div>
            <h1 class="serif-title text-4xl md:text-5xl font-bold mb-4">Happy Birthday!</h1>
            <p class="text-gray-300 mb-8 text-lg">Another year of you making the world brighter. Your existence is a gift, and I'm so lucky to witness it.</p>
            <button onclick="nextStep(3)" class="btn-primary px-8 py-3 rounded-full text-lg">There's more...</button>
        </section>

        <!-- Step 3: Bento Grid Adoration -->
        <section id="step3" class="step-content flex-col items-center text-center max-w-2xl glass-card p-8">
            <h1 class="serif-title text-3xl font-bold mb-8">A Few Things I Adore About You</h1>
            
            <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mb-8 text-left">
                <div class="bento-card md:col-span-2">
                    <h3 class="text-pink-400 font-bold mb-2 flex items-center">✨ Your Unmatched Kindness</h3>
                    <p class="text-sm text-gray-300">The genuine warmth you show to everyone is something truly rare and beautiful. You have a heart that heals.</p>
                </div>
                <div class="bento-card">
                    <h3 class="text-pink-400 font-bold mb-2">😊 That Smile</h3>
                    <p class="text-sm text-gray-300">It's a literal work of art. It can change the entire mood of a room in seconds.</p>
                </div>
                <div class="bento-card">
                    <h3 class="text-pink-400 font-bold mb-2">🔥 Radiant Spirit</h3>
                    <p class="text-sm text-gray-300">Your passion is infectious. Being around you makes everything feel possible.</p>
                </div>
            </div>

            <button onclick="nextStep(4)" class="btn-primary px-8 py-3 rounded-full text-lg">Remember this?</button>
        </section>

        <!-- Step 4: Shared Memory -->
        <section id="step4" class="step-content flex-col items-center text-center max-w-md glass-card p-8">
            <h1 class="serif-title text-3xl font-bold mb-8">That One Time...</h1>
            
            <div class="perspective-container mb-8" id="polaroid-wrapper">
                <div class="polaroid-frame">
                    <img src="https://i.ibb.co/6Z6XgCg/crush.webp" alt="Memory" class="polaroid-img">
                    <p class="text-slate-800 font-handwriting mt-4 text-lg" style="font-family: cursive;">Our favorite memory.</p>
                </div>
            </div>

            <p class="text-gray-300 mb-8">Every moment with you feels like a scene from a movie I'd watch on repeat.</p>
            <button onclick="nextStep(5)" class="btn-primary px-8 py-3 rounded-full text-lg">One last thing...</button>
        </section>

        <!-- Step 5: Finale -->
        <section id="step5" class="step-content flex-col items-center text-center max-w-md glass-card p-8">
            <div class="text-6xl mb-6">🎂</div>
            <h1 class="serif-title text-4xl font-bold mb-4">My Wish For You</h1>
            <p class="text-gray-300 mb-8 text-lg">May the next year bring you all the love, success, and pure happiness you so rightfully deserve. May your dreams soar higher than ever.</p>
            
            <div id="final-message" class="hidden mb-8">
                <h2 class="serif-title text-3xl font-bold">Happy Birthday, my favorite person! 💖</h2>
            </div>

            <button id="celebrate-btn" onclick="startFinale()" class="btn-primary px-12 py-4 rounded-full text-xl">Celebrate!</button>
        </section>

    </main>

    <script>
        // --- Three.js Background Hearts ---
        const scene = new THREE.Scene();
        const camera = new THREE.PerspectiveCamera(75, window.innerWidth / window.innerHeight, 0.1, 1000);
        const renderer = new THREE.WebGLRenderer({ canvas: document.getElementById('three-canvas'), alpha: true, antialias: true });
        renderer.setSize(window.innerWidth, window.innerHeight);

        const hearts = [];
        const heartShape = new THREE.Shape();
        const x = 0, y = 0;
        heartShape.moveTo(x + 5, y + 5);
        heartShape.bezierCurveTo(x + 5, y + 5, x + 4, y, x, y);
        heartShape.bezierCurveTo(x - 6, y, x - 6, y + 7, x - 6, y + 7);
        heartShape.bezierCurveTo(x - 6, y + 11, x - 3, y + 15.4, x + 5, y + 19);
        heartShape.bezierCurveTo(x + 12, y + 15.4, x + 16, y + 11, x + 16, y + 7);
        heartShape.bezierCurveTo(x + 16, y + 7, x + 16, y, x + 10, y);
        heartShape.bezierCurveTo(x + 7, y, x + 5, y + 5, x + 5, y + 5);

        const geometry = new THREE.ShapeGeometry(heartShape);
        const material = new THREE.MeshPhongMaterial({ 
            color: 0xff4d6d, 
            shininess: 100, 
            transparent: true, 
            opacity: 0.6 
        });

        const ambientLight = new THREE.AmbientLight(0xffffff, 0.5);
        scene.add(ambientLight);
        const pointLight = new THREE.PointLight(0xffffff, 1);
        pointLight.position.set(10, 10, 10);
        scene.add(pointLight);

        for (let i = 0; i < 25; i++) {
            const heart = new THREE.Mesh(geometry, material.clone());
            heart.position.set(
                (Math.random() - 0.5) * 100,
                (Math.random() - 0.5) * 100,
                (Math.random() - 0.5) * 50
            );
            heart.rotation.z = Math.PI;
            heart.rotation.x = Math.random() * Math.PI;
            heart.scale.set(0.2, 0.2, 0.2);
            heart.userData = {
                speed: 0.01 + Math.random() * 0.02,
                offset: Math.random() * Math.PI * 2
            };
            hearts.push(heart);
            scene.add(heart);
        }

        camera.position.z = 50;

        function animate() {
            requestAnimationFrame(animate);
            const time = Date.now() * 0.001;
            hearts.forEach(heart => {
                heart.position.y += Math.sin(time + heart.userData.offset) * 0.05;
                heart.position.x += Math.cos(time + heart.userData.offset) * 0.02;
                heart.rotation.y += heart.userData.speed;
            });
            renderer.render(scene, camera);
        }
        animate();

        // --- Navigation Logic ---
        let currentStep = 1;
        const totalSteps = 5;

        function nextStep(step) {
            const currentEl = document.getElementById(`step${currentStep}`);
            const nextEl = document.getElementById(`step${step}`);

            gsap.to(currentEl, {
                opacity: 0,
                y: -20,
                duration: 0.5,
                onComplete: () => {
                    currentEl.classList.remove('active');
                    nextEl.classList.add('active');
                    currentStep = step;
                    
                    // Update Progress Bar
                    gsap.to("#progress-bar", {
                        width: `${(step / totalSteps) * 100}%`,
                        duration: 0.6
                    });

                    gsap.fromTo(nextEl, 
                        { opacity: 0, y: 20, scale: 0.95 },
                        { opacity: 1, y: 0, scale: 1, duration: 0.6, ease: "back.out(1.7)" }
                    );

                    // Special stagger for Step 3 Bento
                    if(step === 3) {
                        gsap.from(".bento-card", {
                            opacity: 0,
                            y: 20,
                            stagger: 0.2,
                            delay: 0.3
                        });
                    }
                }
            });
        }

        // --- Polaroid Parallax ---
        const polaroid = document.getElementById('polaroid-wrapper');
        document.addEventListener('mousemove', (e) => {
            if(currentStep !== 4) return;
            const x = (window.innerWidth / 2 - e.pageX) / 25;
            const y = (window.innerHeight / 2 - e.pageY) / 25;
            gsap.to(polaroid, {
                rotationY: x,
                rotationX: -y,
                duration: 0.5
            });
        });

        // --- Grand Finale ---
        function startFinale() {
            const btn = document.getElementById('celebrate-btn');
            const finalMsg = document.getElementById('final-message');
            btn.style.pointerEvents = 'none';
            
            gsap.to(btn, { opacity: 0, scale: 0.5, duration: 0.5 });
            
            setTimeout(() => {
                finalMsg.classList.remove('hidden');
                gsap.fromTo(finalMsg, 
                    { opacity: 0, y: 20 },
                    { opacity: 1, y: 0, duration: 1 }
                );
            }, 500);

            // Confetti Sequence
            const duration = 5 * 1000;
            const animationEnd = Date.now() + duration;
            const defaults = { startVelocity: 30, spread: 360, ticks: 60, zIndex: 0 };

            function randomInRange(min, max) {
                return Math.random() * (max - min) + min;
            }

            const interval = setInterval(function() {
                const timeLeft = animationEnd - Date.now();

                if (timeLeft <= 0) {
                    return clearInterval(interval);
                }

                const particleCount = 50 * (timeLeft / duration);
                
                // Bursts from corners
                confetti({ ...defaults, particleCount, origin: { x: randomInRange(0.1, 0.3), y: Math.random() - 0.2 } });
                confetti({ ...defaults, particleCount, origin: { x: randomInRange(0.7, 0.9), y: Math.random() - 0.2 } });
                
                // Emoji confetti
                confetti({
                    ...defaults,
                    particleCount: 10,
                    scalar: 2,
                    shapes: ['text'],
                    shapeOptions: { text: { value: ['💖', '✨', '🎈'] } }
                });
            }, 250);

            // Three.js Hearts Exodus
            hearts.forEach((heart, i) => {
                gsap.to(heart.position, {
                    x: (Math.random() - 0.5) * 200,
                    y: 100,
                    z: (Math.random() - 0.5) * 200,
                    duration: 3 + Math.random() * 2,
                    ease: "power2.in",
                    delay: i * 0.05
                });
                gsap.to(heart.material, {
                    opacity: 0,
                    duration: 2,
                    delay: 2 + i * 0.05
                });
            });
        }

        // Handle Resize
        window.addEventListener('resize', () => {
            camera.aspect = window.innerWidth / window.innerHeight;
            camera.updateProjectionMatrix();
            renderer.setSize(window.innerWidth, window.innerHeight);
        });
    </script>
</body>
</html>